<?xml version="1.0" encoding="utf-8"?>
<!--
    权限弹窗布局 - 现代MIUI风格
    大圆角卡片 + 渐变背景 + 时尚设计
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <!-- 居中弹窗卡片 -->
    <LinearLayout
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@drawable/bg_dialog_card"
        android:elevation="24dp"
        android:orientation="vertical"
        android:paddingLeft="24dp"
        android:paddingTop="32dp"
        android:paddingRight="24dp"
        android:paddingBottom="0dp">

        <!-- 应用图标（带边框阴影） -->
        <FrameLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal">

            <ImageView
                android:id="@+id/iv_app_icon"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:src="@drawable/ic_app_default"
                android:scaleType="fitCenter" />
        </FrameLayout>

        <!-- 应用名称 -->
        <TextView
            android:id="@+id/tv_app_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            android:textColor="@color/text_primary"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- 倒计时显示（更醒目） -->
        <TextView
            android:id="@+id/tv_countdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="6dp"
            android:text="@string/countdown_initial"
            android:textColor="@color/color_primary"
            android:textSize="14sp" />

        <!-- 权限说明文字 -->
        <TextView
            android:id="@+id/tv_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:lineSpacingMultiplier="1.3"
            android:textColor="@color/text_secondary"
            android:textSize="15sp" />

        <!-- 内容预览（圆角背景） -->
        <TextView
            android:id="@+id/tv_content_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_content_preview"
            android:maxLines="3"
            android:ellipsize="end"
            android:padding="12dp"
            android:textColor="@color/text_hint"
            android:textSize="13sp"
            android:visibility="gone" />

        <!-- 分隔线 -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
            android:layout_marginTop="20dp"
            android:background="@color/divider_color" />

        <!-- 底部按钮区域 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:orientation="horizontal">

            <!-- 拒绝按钮 -->
            <TextView
                android:id="@+id/btn_deny"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:clickable="true"
                android:focusable="true"
                android:background="@drawable/btn_deny_bg"
                android:gravity="center"
                android:text="@string/btn_deny"
                android:textColor="@color/btn_deny_text"
                android:textSize="16sp" />

            <!-- 按钮间分隔线 -->
            <View
                android:layout_width="1px"
                android:layout_height="match_parent"
                android:background="@color/divider_color" />

            <!-- 允许按钮 -->
            <TextView
                android:id="@+id/btn_allow"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:clickable="true"
                android:focusable="true"
                android:background="@drawable/btn_allow_bg"
                android:gravity="center"
                android:text="@string/btn_allow"
                android:textColor="@color/btn_allow_text"
                android:textSize="16sp"
                android:textStyle="bold" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>