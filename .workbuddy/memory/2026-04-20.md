# 2026-04-20 工作日志

## #1 首次使用权限引导弹窗 - 已完成 ✓（v3 添加自启动项）
- **新增布局** `dialog_first_launch_guide.xml`：三项检测卡片（悬浮窗 + 自启动 + Xposed）
- **strings.xml**：13个权限相关文案
  - 新增 guide_autostart_perm, guide_autostart_perm_desc, guide_autostart_hint
- **MainActivity.java**：
  - 三项引导内容：
    1. 悬浮窗权限 (Settings.canDrawOverlays) → 已开启✓ / 未开启→跳转系统设置
    2. 自启动/关联启动 → 提示性文字（各厂商路径不同，无法程序化检测），斜体灰色提示
    3. Xposed 模块激活 (isModuleActive()) → 已激活✓ / 未激活提示LSPosed
  - 按钮根据悬浮窗状态动态显示"去设置"/"稍后再说"

## #3 引导弹窗权限状态刷新 - 已完成 ✓
- **问题**：用户从桌面切到设置开启/关闭悬浮窗权限，返回App后弹窗状态不刷新
- **修复**：
  1. 使用 `mGuideView` 成员变量保存弹窗自定义布局的 View 引用
  2. `onResume` 中通过 `mGuideView.findViewById()` 直接更新弹窗内容
  3. 去掉 `postDelayed(100ms)` 延迟，改为同步执行
  4. 去掉 `mGuideDialogShown` 标记，简化逻辑
- **关键**：AlertDialog 的 `findViewById()` 无法找到 `setView()` 添加的自定义布局，必须保存原始 View 引用
- **新增布局** `page_permission_detail.xml`：独立的权限详情页面
  - 悬浮窗权限卡片：图标 + 说明 + 状态 + 跳转按钮
  - 自启动权限卡片：图标 + 说明 + 提示 + 跳转按钮
  - Xposed模块状态卡片：图标 + 说明 + 状态
- **新增资源**：
  - `ic_float_window.xml` - 悬浮窗图标
  - `ic_autostart.xml` - 自启动图标
  - `ic_xposed.xml` - Xposed图标
  - `bg_icon_circle.xml` - 圆形图标背景
  - `ic_permission.xml` - 权限设置入口图标
- **修改** `page_settings.xml`：权限状态改为可点击入口行
- **修改** `activity_main.xml`：添加权限详情页 include
- **修改** `MainActivity.java`：
  - 新增 `PAGE_PERMISSION_DETAIL = 4` 常量
  - `showPage()` 支持权限详情页
  - 新增 `initPermissionDetailPage()` 方法
  - 设置页权限入口点击跳转到权限详情页
