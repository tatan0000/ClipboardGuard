# 2026-04-08 工作日志

## 功能规划会议

用户确认了以下待实现功能的优先级：

### 5 待开发功能（按计划顺序）
1. **日志功能完善**：Hook 里条件写入日志数据库，日志页面展示，设置页右上角加开关
2. **系统核心应用排除**：应用选择页面过滤掉系统核心包，与 Hook 行为一致
3. **全选/全不选 + 保存提示**：ExpandableListView 上加按钮，Toast 改为「已保存，拦截 N 个应用」
4. **首次引导 + 权限检查**：首次启动引导用户授权，设置页加权限检查入口（悬浮窗/关联启动/模块激活等）
5. **system_server 内弹窗**：用 WindowManager.addView() 替代 Activity 冷启动，实现零延迟弹窗

### 其他讨论
- 日志页面已存在，但日志开关和输出逻辑未实现
- 系统核心应用排除不一致问题：Hook 已排但页面仍显示
- Shizuku 版本技术路线不通（Shizuku 无法拦截剪贴板写入）
- Magisk 模块可行但工作量巨大，暂缓

## 2026-04-18 新增 Issue 功能分析

### Issue 10：正则匹配规则（写入剪贴板）
- 新增 rules 表（package_name/regex/action/priority）
- Hook 里提取内容后先跑正则，再决定放行/拦截/弹窗
- UI 加规则管理页面
- 难度：⭐⭐⭐，工作量：中

### Issue 11：读剪贴板权限控制
- Hook getPrimaryClip，配合正则规则
- 注意 getPrimaryClip 调用频率极高（输入法、系统UI等），必须配合正则过滤
- 拦截时返回假的 ClipData（比写入拦截更复杂）
- 难度：⭐⭐⭐⭐，工作量：大
- 建议与 Issue 10 一起做，复用 rules 表（加 apply_to_read 字段）

## 2026-04-18 完成功能
- ✅ 任务4：保存提示改为「已保存，拦截 N 个应用」/ 「已保存，未拦截任何应用」
- ✅ 任务3：应用选择页添加全选/全不选按钮，并显示「已选 N 个拦截」计数
- ✅ 任务2（升级版）：Thanox 全局白名单（约90个系统核心包），Hook 和 MainActivity 共用同一套过滤逻辑
  - `res/values/arrays.xml` 新增 `global_whitelist_packages` 数组
  - `MainActivity.java` 改为从资源读取 + `HashSet` + 前缀匹配
  - `Hook.java` 改为 `isSystemCorePackage()` 方法 + 静态 `HashSet` 初始化

## 今日修改
- 修复编译错误（添加 Context 导入）
- Toolbar 高度从 80dp 调整为 100dp
- RadioButton 改回默认样式

## 2026-04-18 完成功能
- ✅ 任务4：保存提示改为「已保存，拦截 N 个应用」/ 「已保存，未拦截任何应用」
- ✅ 任务3：应用选择页添加全选/全不选按钮，并显示「已选 N 个拦截」计数
- ✅ 任务2：loadAllApps() 加 isCoreSystemPackage() 过滤，排除 android/systemui/settings 等核心系统包

