# 2026-04-19 工作日志

## UI 微调 - Toolbar 高度 + 底部导航栏高度
- Toolbar: 68dp → 72dp; bottom_nav: 60dp → 62dp; 所有页面间距/FAB 同步调整 ✓

## 全选/反选按钮恢复
- Java 逻辑 `setAllAppsBlocked()` 一直保留着，但布局按钮和事件绑定丢失
- 恢复内容：
  - strings.xml：新增 `btn_select_all` / `btn_deselect_all`
  - activity_main.xml：搜索框和提示条之间添加两个 MaterialButton（outlined 圆角胶囊样式）
  - MainActivity.java：findViewById + setOnClickListener 调用已有方法，补了 MaterialButton import
- #4 标记为 completed

## Todo 列表更新
- #3 系统核心应用排除/过滤一致性修复 标记为 completed（昨天已完成但漏记）
- #4 全选/反选按钮 标记为 completed（今天恢复）
- #7 保存计数提示优化 标记为 completed（之前漏记）
- **Toolbar**：68dp → **72dp**（标题被状态栏遮挡，加高使其完整显示）
- **bottom_nav 高度**：60dp → **62dp**
- **首页/应用页 paddingBottom**：60dp → **62dp**
- **日志页/设置页 marginBottom**：60dp → **62dp**
- **FAB marginBottom**：76dp → **78dp**（同步底栏上移）

## 新功能规划（2026-04-19）
- **正则匹配弹窗优化**：用户配置正则规则（如 `\d{11}` 手机号），只有剪贴板内容匹配时才弹窗询问，否则直接放行
- **读剪贴板权限控制**：Hook `getPrimaryClip()` 监控读取行为，参考写入权限模式，支持正则匹配应用包名
